/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // IMPORTANT: nu folosi "output: 'export'" dacă vrei /api/*
  // adaugă aici orice alte setări pe care le aveai, dar fără "output: 'export'"
};

export default nextConfig;
